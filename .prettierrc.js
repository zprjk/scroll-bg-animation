module.exports = {
  // Code's length that can reach horizontally
  printWidth: 80,

  // Add a ';' at the end of every statement
  semi: true,

  // Include "()" around a sole arrow function parameter.
  arrowParens: "always",

  // "" instead of ''
  singleQuote: true,

  // Put ',' where valid in ES5 (objects, arrays, etc.)
  trailingComma: "es5",

  // lf instead of 'crlf'
  endOfLine: "lf",

  // `{Button}` instead of `{ Button }`
  bracketSpacing: false,

  // markdown text wrap if it exceeds the print width
  proseWrap: "always",
};
